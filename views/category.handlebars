<section class="container">
  <h3 class="category-name">{{category.name}}</h3>
  <section class="flex-row justify-center">
    {{#each category.pets as |pet|}}
    {{> pet-details}}
    {{/each}}
  </section>
</section>